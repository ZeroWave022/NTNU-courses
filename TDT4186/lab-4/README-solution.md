# TDT4186: Lab 4

Performed changes:

- Implemented functions in `user/uthread.c`
- Added per-process data structures and adjusted the `_main` function in `user/ulib.c`
- Adjusted the `thread` struct in `user/user.h`
